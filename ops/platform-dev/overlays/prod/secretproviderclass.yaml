apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: content-library-secrets
spec:
  provider: gcp
  parameters:
    secrets: |
      - resourceName: "projects/content-library-development/secrets/SECRET_ENV_FILE/versions/latest"
        path: ".env"
