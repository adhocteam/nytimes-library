apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-library
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: content-library
    spec:
      containers:
      - name: content-library
        image: gcr.io/adhoc-dev-service-platform/content-library:prod
